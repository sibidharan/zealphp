<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         executionOrder="default"
         stopOnFailure="false">
  <testsuites>
    <testsuite name="Unit">
      <directory>tests/Unit</directory>
    </testsuite>
    <testsuite name="Integration">
      <directory>tests/Integration</directory>
    </testsuite>
  </testsuites>
  <source>
    <include>
      <directory>src</directory>
    </include>
    <exclude>
      <file>src/pool_worker.php</file>
      <file>src/cgi_worker.php</file>
      <file>src/fork_master.php</file>
    </exclude>
  </source>
  <php>
    <env name="ZEALPHP_REDIS_URL" value="redis://127.0.0.1:16379/0"/>
    <env name="ZEALPHP_MEMCACHED_SERVERS" value="127.0.0.1:11211"/>
    <!-- MySQL/MariaDB for the DbConnectionPool mysqli-driver tests
         (tests/Unit/Db/MysqliPoolTest). Matches the CI `mariadb` service
         (root, empty password, db 'zealtest'). Tests skip gracefully when no
         server is reachable, so local runs without MySQL are non-fatal. -->
    <env name="ZEALPHP_TEST_MYSQL_HOST" value="127.0.0.1"/>
    <env name="ZEALPHP_TEST_MYSQL_PORT" value="3306"/>
    <env name="ZEALPHP_TEST_MYSQL_USER" value="root"/>
    <env name="ZEALPHP_TEST_MYSQL_PASS" value=""/>
    <env name="ZEALPHP_TEST_MYSQL_DB" value="zealtest"/>
    <!-- AppCompatibilityTest probes an external Docker app-lab (scripts/app-lab/)
         that isn't available in CI or most dev boxes. Skip by default; set
         SKIP_LAB_TESTS=0 + ZEALPHP_LAB_BASE_URL in the shell env to enable. -->
    <env name="SKIP_LAB_TESTS" value="1"/>
  </php>
</phpunit>
